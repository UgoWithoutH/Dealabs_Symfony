{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    {{ form_start(form) }}
    <div>
        {{ form_errors(form) }}
    </div>

    <div class="row">
        <div class="col">
            {{ form_row(form.pseudo) }}
        </div>
        <div class="col">
            {{ form_row(form.motDePasse) }}
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}